Swap the homepage ticker labels so the naming hierarchy is clearer.

## Changes
1. In `src/components/home/member-home.tsx`, change the `GroupNewsTicker` label from `"Workshop now"` to `"Latest"`.
2. In the same file, change the mobile `HomeSection` title from `"Now"` to `"Workshop Now"`.

These two edits swap the labels: the top ambient pill becomes the "Latest" news feed, and the mobile departures-board module becomes the "Workshop Now" board.
